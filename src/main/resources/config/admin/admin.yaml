server:
  port: 9090

spring:
  application:
    name: admin
  security:
    user:
      name: ${USER_NAME:admin}
      password: ${USER_PASSWORD:asdf}

eureka:
  instance:
    metadata-map:
      user.name: ${spring.security.user.name}
      user.password: ${spring.security.user.password}