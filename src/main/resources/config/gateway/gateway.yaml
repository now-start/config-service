server:
  port: 8000

spring:
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
    config:
      discovery:
        enabled: true

management:
  endpoint:
    gateway:
      enabled: true